interface:
  display_name: "Wan2GP Operator"
  short_description: "Codex/Claude-compatible WanGP suite: bootstrap install, launch UI, choose current video models, compose jobs, run headless."
  default_prompt: "Use wan2gp-operator via scripts/wan2gp_operator.py to choose current WanGP model targets, bootstrap WanGP, and operate it from terminal."

policy:
  allow_implicit_invocation: true

